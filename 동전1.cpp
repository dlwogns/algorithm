#include <iostream>
#include <vector>
using namespace std;
int d1[10000];
int d2[10000];
int main() {

	int n, k;
	cin >> n >> k;
	vector<int>a(n);
	for (size_t i = 0; i < n; i++)
		cin >> a[i];
	
	for (size_t i = 0; i < n; i++)
	{
		for (size_t j = 0; j < k; j++)
		{

		}
	}

}